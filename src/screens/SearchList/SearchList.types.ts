import type {StackScreenProps} from '@react-navigation/stack';
import type {StackParamList} from '../../routers/Stack';

export type SearchListScreenProps = StackScreenProps<
  StackParamList,
  'SearchListScreen'
>;
